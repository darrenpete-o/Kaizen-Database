name: Update Database Diagram

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 6 * * 0'
  workflow_dispatch:
  
jobs:
  update-diagram:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: false
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyodbc pymssql openai pandas openpyxl google-genai cerebras-cloud-sdk
          echo "✅ Installed packages:"
          pip list | findstr "pyodbc pymssql openai pandas openpyxl google-genai cerebras-cloud-sdk"

      - name: Verify OpenAI installation
        run: |
          python -c "import openai; print(f'✅ OpenAI version: {openai.__version__}')"

      - name: Verify Cerebras installation
        run: |
          python -c "from cerebras.cloud.sdk import Cerebras; print('✅ Cerebras installed successfully')"
          
      - name: Verify ODBC Driver
        run: |
          python -c "import pyodbc; print('Available ODBC drivers:'); print(pyodbc.drivers())"

      - name: Check for Changes
        env:
          DB_CONNECTION_STRING: ${{ secrets.DB_CONNECTION_STRING }}
        run: python check_for_changes.py

      # Upload baseline for future runs - ONLY if script succeeded
      - name: Upload baseline for next run
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: database-baseline
          path: database_baseline.json

      # Upload Excel report - ALWAYS (even if script had warnings)
      - name: Upload Excel Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: database-changes
          path: database_changes_*.xlsx
        
      - name: Generate view definitions
        env:
          DB_CONNECTION_STRING: ${{ secrets.DB_CONNECTION_STRING }}
        run: python generate_sql_definitions.py

      - name: Generate table definitions
        env:
          DB_CONNECTION_STRING: ${{ secrets.DB_CONNECTION_STRING }}
        run: python generating_schema_diagram.py

      - name: Generate business friendly names as dbml
        env:
          DB_CONNECTION_STRING: ${{ secrets.DB_CONNECTION_STRING }}
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
          PYTHONIOENCODING: utf-8
          PYTHONUTF8: 1
        run: python generate_business_friendly_names.py

      - name: Verify DBML file
        run: |
          if (Test-Path "schema_diagram_with_aliases_as_notes.dbml") {
            $content = Get-Content "schema_diagram_with_aliases_as_notes.dbml" -Raw
            Write-Host "✅ DBML file exists. Size: $($content.Length) characters"
          } else {
            Write-Host "❌ DBML file not found!"
            exit 1
          }

      - name: Debug - Show current directory and files
        run: |
          Write-Host "Current directory: $(Get-Location)"
          Write-Host "Files in current directory:"
          Get-ChildItem -File
          Write-Host "`nFull path of DBML file:"
          Resolve-Path "schema_diagram_with_aliases_as_notes.dbml" -ErrorAction SilentlyContinue

      - name: Run Business Name Check For Database Alterations
        env:
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
        run: |
          python naming_standard_restrictions.py
        continue-on-error: true

      # ================================================
      # NEW: dbdiagram CLI STEPS (The ones that worked in PowerShell)
      # ================================================
#      - name: Setup Node.js
#        uses: actions/setup-node@v4
#        with:
#          node-version: '24' # Or any LTS version like '20' or '18'
          
#      - name: Install dbdiagram CLI
#        run: |
#          Write-Host "Installing dbdiagram CLI..."
#          npm install -g dbdiagram
#          dbdiagram --version
#        shell: powershell

#      - name: Rename DBML file to schema.dbml
#        run: |
#          Write-Host "Renaming DBML file to schema.dbml..."
#          Copy-Item "schema_diagram_with_aliases_as_notes.dbml" "schema.dbml" -Force
#          Write-Host "schema.dbml created"
#        shell: powershell

      #- name: Step 1 - Pull first (get latest layout)
        #run: |
          #Write-Host "Pulling latest layout from dbdiagram.io..."
          #dbdiagram pull --diagram-id ${{ secrets.DIAGRAM_ID }} -o schema.dbml
          #Write-Host "Layout pulled successfully"
        #shell: powershell
        #env:
          #DBDIAGRAM_TOKEN: ${{ secrets.DBDIAGRAM_TOKEN }}

 #     - name: Step 2 - Verify companion file exists
 #       run: |
#          Write-Host "Checking companion file..."
#          if (Test-Path "schema.dbdiagram") {
#            $size = (Get-Item "schema.dbdiagram").Length
#            Write-Host "schema.dbdiagram exists. Size: $size bytes"
#          } else {
#            Write-Host "schema.dbdiagram not found - will be created on push"
#          }
#        shell: powershell
#
#      - name: Step 3 - Push updated DBML (preserves positions)
#        run: |
#          Write-Host "Pushing updated schema to dbdiagram.io..."
#          dbdiagram push schema.dbml --diagram-id ${{ secrets.DIAGRAM_ID }}
#          Write-Host "Schema pushed successfully"
#        shell: powershell
#        env:
#          DBDIAGRAM_TOKEN: ${{ secrets.DBDIAGRAM_TOKEN }}

#      - name: Step 4 - Pull again (capture any layout changes)
#        run: |
#          Write-Host "Capturing final layout..."
#          dbdiagram pull --diagram-id ${{ secrets.DIAGRAM_ID }} -o schema.dbml
#          Write-Host "Layout captured successfully"
#        shell: powershell
#        env:
#          DBDIAGRAM_TOKEN: ${{ secrets.DBDIAGRAM_TOKEN }}

#      - name: Step 5 - Commit companion file
#        run: |
#          Write-Host "Committing companion file to repository..."
    
          # Set the remote URL with the token
#          git remote set-url origin https://${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
    
#          git config user.name "GitHub Actions"
#          git config user.email "actions@github.com"
    
#          if (Test-Path "schema.dbdiagram") {
#            git add schema.dbdiagram
#            git diff --cached --quiet
#            if ($LASTEXITCODE -ne 0) {
#              git commit -m "chore: update dbdiagram companion file with layout positions [skip ci]"
#              Write-Host "Pushing to GitHub..."
#              git push --force
#              Write-Host "Companion file committed and pushed"
#            } else {
#              Write-Host "No changes to companion file"
#            }
#          } else {
#            Write-Host "schema.dbdiagram not found - nothing to commit"
#          }
#        shell: powershell  
#        timeout-minutes: 3

      - name: Upload DBML to dbdiagram
        timeout-minutes: 2
        env:
          API_TOKEN: ${{ secrets.DBDIAGRAM_API_TOKEN }}
          DIAGRAM_ID: ${{ secrets.DIAGRAM_ID }}
        shell: powershell
        run: |
          Write-Host "Reading DBML file..."
    
          $filePath = "C:\actions-runner\_work\Kaizen-Database\Kaizen-Database\schema_diagram_with_aliases_as_notes.dbml"
    
          if (-not (Test-Path $filePath)) {
            Write-Host "File not found!"
            exit 1
          }
    
          $content = [System.IO.File]::ReadAllText($filePath)
          Write-Host "Read $($content.Length) characters"
          Write-Host "Uploading to dbdiagram.io..."
    
          $body = @{
            name = "Kaizen Database Schema Diagram"
            content = $content
          } | ConvertTo-Json -Depth 10
    
          $headers = @{
            "dbdiagram-access-token" = $env:API_TOKEN
            "Content-Type" = "application/json"
          }
    
          $response = Invoke-RestMethod -Method Put -Uri "https://api.dbdiagram.io/v1/diagrams/$env:DIAGRAM_ID" -Headers $headers -Body $body -TimeoutSec 30
    
          Write-Host "Upload successful!"
          Write-Host "View your diagram at: https://dbdiagram.io/d/$env:DIAGRAM_ID"
          
      - name: Step 6 - Verify deployment
        run: |
          Write-Host "Deployment complete!"
          Write-Host "View your diagram at: https://dbdiagram.io/d/${{ secrets.DIAGRAM_ID }}"
          Write-Host ""
          Write-Host "Companion file info:"
          if (Test-Path "schema.dbdiagram") {
            Get-Item "schema.dbdiagram" | Format-List Name, Length, LastWriteTime
          } else {
            Write-Host "No companion file found"
          }
        shell: powershell
